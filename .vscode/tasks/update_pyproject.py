#!/usr/bin/env python3

"""Module to update the pyproject to have the same values in the [project] and [tool.poetry] key."""

import os
from os import PathLike
from rich import print as pprint, inspect
